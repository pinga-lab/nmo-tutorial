Figure 2: In the left is the $v_{NMO}$ profile passed to
\texttt{nmo_correction}}.
The profile was interpolated on a line using the two picked velocities (black
squares). In the middle is a synthetic CMP gather. In the right is the output
from our \texttt{nmo_correction} function.
